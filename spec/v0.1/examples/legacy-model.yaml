schema_version: "2.0"
standard: other
biosim:
  entrypoint: src.model:Model
  communication_step: 1.0
io:
  inputs:
    - name: expression
      signal_type: array
      dtype: float32
      shape: ["*"]
  outputs: []
